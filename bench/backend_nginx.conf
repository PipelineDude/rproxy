events {
    worker_connections 4096;
}
http {
    access_log off;
    server {
        listen 8080;
        location / {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}
